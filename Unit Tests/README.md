## This folder contains Valdation tests done to check sensors/hardware parts

The tests are split into two main categories:
* **Display_Tests**: Scripts to verify the touchscreen and display functionality.
* **Printer_Tests**: Scripts to verify the printer connectivity and spooler status.

To run a test, navigate to the relevant folder in the terminal and run:
python3 [filename].py
